<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EAF0FF"
    tools:context=".ui.SplashActivity"
    android:fitsSystemWindows="true">

    <!-- Frame container to overlap subtitle on title -->
    <FrameLayout
        android:id="@+id/logoGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:ignore="ExtraText">

        <!-- Main App Name -->
        <TextView
            android:id="@+id/appName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="जीवनमान"
            android:textColor="#6A040F"
            android:textSize="52sp"
            android:textStyle="bold"
            android:paddingTop="5dp"
            android:fontFamily="@font/tiro_devanagari_marathi_regular"
            android:letterSpacing="1.02"
            android:layout_gravity="center"
            android:shadowColor="#55000000"
            android:shadowDx="1"
            android:shadowDy="1"
            android:shadowRadius="1"/>

        <!-- Subtitle positioned over top-end of app name -->
        <TextView
            android:id="@+id/subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="जनसामाण्यांचे"
            android:textColor="#2F2F2F"
            android:textSize="16sp"
            android:fontFamily="@font/tiro_devanagari_marathi_regular"
            android:letterSpacing="1.01"
            android:layout_gravity="end|top"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="21dp"/>
    </FrameLayout>

    <!-- Powered by / Credit -->
    <TextView
        android:id="@+id/poweredBy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Powered by Arjun Dabbe"
        android:textColor="#666666"
        android:textSize="12sp"
        android:fontFamily="sans-serif"
        android:letterSpacing="0.02"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="12dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
