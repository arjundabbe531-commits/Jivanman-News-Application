<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:layout_marginBottom="8dp"
    android:background="?attr/selectableItemBackground"
    >

    <!-- Article Image -->
    <ImageView
        android:id="@+id/articleImage"
        android:layout_width="120dp"
        android:layout_height="80dp"
        android:scaleType="centerCrop"
        android:contentDescription="Article Image"
        android:background="@drawable/rounded_corners"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <!-- Title -->
    <TextView
        android:id="@+id/articleTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="भारत 2025 मध्ये जगातील सर्वांत वेगाने वाढणारी अर्थव्यवस्था!"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/news_headline_text"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/articleImage"
        app:layout_constraintTop_toTopOf="@id/articleImage"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <!-- Description -->
    <TextView
        android:id="@+id/articleDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:text="भारताची GDP वाढ दर 6.5% — चीन, अमेरिका व युरोपच्या पुढे!"
        android:textSize="14sp"
        android:textColor="@color/news_description_text"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/articleImage"
        app:layout_constraintTop_toBottomOf="@id/articleTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/articleImage"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
